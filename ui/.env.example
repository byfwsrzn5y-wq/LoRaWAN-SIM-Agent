# Target simulator control server when using Vite dev proxy (see vite.config.ts)
VITE_CONTROL_PROXY_TARGET=http://127.0.0.1:9999

# Production or direct API: full origin, no trailing slash (bypasses proxy; set when UI is served separately)
# VITE_CONTROL_API_BASE=http://127.0.0.1:9999
